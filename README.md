# guidelines

1.Comments
Use JSDoc style comments for functions, interfaces, enums, and classes.
2. Strings
a) Use double quotes for strings.
b) All strings visible to the user need to be localized (make an entry in diagnosticMessages.json).
